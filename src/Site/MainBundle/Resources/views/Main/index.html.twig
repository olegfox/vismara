{% extends 'SiteMainBundle::layout.html.twig' %}

{% block title %}{% if app.request.locale == "it" %}{{ page.titleIt }}{% elseif app.request.locale == "ru" %}{{ page.titleRu }}{% else %}{{ page.title }}{% endif %}{% endblock %}
{% block keywords %}{% if app.request.locale == "it" %}{{ page.keywordIt }}{% elseif app.request.locale == "ru" %}{{ page.keywordRu }}{% else %}{{ page.keyword }}{% endif %}{% endblock %}
{% block description %}{% if app.request.locale == "it" %}{{ page.descriptionIt }}{% elseif app.request.locale == "ru" %}{{ page.descriptionRu }}{% else %}{{ page.description }}{% endif %}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('sitemain/css/flexslider.css') }}" rel="stylesheet">
    <link href="{{ asset('sitemain/css/flexslider-custom.css') }}" rel="stylesheet">
    {% if page.background.id is defined %}
    <style type="text/css">
        .wrapper-img {
            background: url("{% path page.background, 'reference' %}") top center;
        }
    </style>
    {% endif %}
{% endblock %}
    {% block slider %}
        {% if sliders is defined %}
                <div class="slider slogan layout flexslider">
                    <ul class="slides">
                        {% for slider in sliders %}
                            <li>
                                {% if slider.link != "" %}
                                    <a href="{{ slider.link }}">
                                        <img src="{% path slider.img, 'reference' %}"
                                             alt="{% path slider.img, 'reference' %}"/>
                                    </a>
                                {% else %}
                                    <img src="{% path slider.img, 'reference' %}"
                                         alt="{% path slider.img, 'reference' %}"/>
                                {% endif %}
                                <div class="text" {% if slider.link != "" %}onclick="window.location.href='{{ slider.link }}';"{% endif %}>
                                    <div class="table">
                                        <div class="table-cell{% if slider.dark == false %} dark{% endif %}">
                                            <h1 class="slogan-title">{% if app.request.locale == 'en' %}{{ slider.text|raw }}{% elseif app.request.locale == 'it' %}{{ slider.textIt|raw }}{% elseif app.request.locale == 'ru' %}{{ slider.textRu|raw }}{% endif %}</h1>
                                        </div>
                                    </div>
                                </div>

                            </li>
                        {% endfor %}
                    </ul>
                </div>
        {% endif %}
    {% endblock %}


{% block content %}
    <div class="main layout">
        {% if page is defined %}
            {% if app.request.locale == 'en' %}
                {{ page.text|raw }}
            {% elseif app.request.locale == 'it' %}
                {{ page.textIt|raw }}
            {% elseif app.request.locale == 'ru' %}
                {{ page.textRu|raw }}
            {% endif %}
        {% endif %}
    </div>
    <div class="wrapper-collection layout-max">
        <div class="collection layout">
            <h2 class="collection-title">{{ 'collection'|trans }}</h2>
            <div class="gallery group">
                {% include 'SiteMainBundle:Gallery:catalog.html.twig' %}
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    <!-- script -->
    {{ parent() }}
    <script src="{{ asset('sitemain/js/script.js') }}"></script>
    <script src="{{ asset('sitemain/js/jquery.flexslider-min.js') }}"></script>
    {% if app.request.locale == 'ru'%}
        <script src="{{ asset('sitemain/js/flexslider_ru.js') }}"></script>
    {% else %}
        <script src="{{ asset('sitemain/js/flexslider.js') }}"></script>
    {% endif %}
    <!-- end script -->
{% endblock %}